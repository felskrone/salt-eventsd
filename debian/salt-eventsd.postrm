#!/bin/bash

# Automatically added by dh_installinit
if [ "$1" = "purge" ] ; then
	update-rc.d salt-eventsd remove >/dev/null
	rm -vrf /etc/salt/eventsd*
	rm -vrf /var/run/salt-eventsd*
fi
# End automatically added section
